apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "humongous-healthcare-api.fullname" . }}-test-connection"
  labels:
    {{- include "humongous-healthcare-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "humongous-healthcare-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
